[tool.poetry]
name = 'orchestra-agent'
version = '1.0.0'
description = 'A distributed multi-agent orchestration framework'
authors = ['Orchestra Agent <orchestra-agent@github.com>']

[tool.poetry.dependencies]
python = '^3.9'

[tool.poetry.dev-dependencies]
pytest = '^6.2.5'